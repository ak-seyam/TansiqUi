import { makeStyles } from "@material-ui/core";

export const useCRAStyles = makeStyles({
  root: {
    paddingRight: "24px",
    width: "400px",
    display: "flex",
    justifyContent: "center",
    flexDirection: "column",
    alignItems: "center",
  },
});
